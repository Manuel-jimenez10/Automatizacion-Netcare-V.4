import { Router } from 'express';
import { taskCompleted } from '../controllers/whatsapp.controller';

const router = Router();

// EspoCRM → API
// router.post('/task-completed', taskCompleted);

export default router;
